#include <stdio.h>
/**
 * main - a function prints the number of arguments.
 * @argc: the number of arguments.
 * @argv: array containing all the strings that ar passed to the array.
 * Return: 0.
 */
int main(int argc, char **argv)
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
