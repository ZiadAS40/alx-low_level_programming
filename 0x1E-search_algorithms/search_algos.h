#ifndef SEARCH_ALGOS
#define SEARCH_ALGOS

/* defint the libiraries */

#include <stdio.h>
#include <stdlib.h>
#include <math.h>


/* the functions prototypes */
int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);


#endif